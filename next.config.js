const nextConfig = {
  // output: "export", // Экспортируем как статические файлы
  //  images: {
  //   unoptimized: true,
  // },
  // distDir: "out", // Папка сборки
};
module.exports = nextConfig;
